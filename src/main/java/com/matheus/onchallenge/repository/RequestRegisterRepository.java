package com.matheus.onchallenge.repository;

import com.matheus.onchallenge.model.RequestRegister;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestRegisterRepository extends JpaRepository<RequestRegister, Long> {
}
